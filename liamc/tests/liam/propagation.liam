//Oh no an error occured
//0

import "stdlib/basic.liam";

struct Error {
    message: str
}

fn main() u64 {

    let err := maybe_error(9u64) return u64 else Error;
    println[str](err.message);

    let x := give_me_union() return u64 else bool | str;
    
    if x is bool as b {
        println[bool](*b);
    }
}

fn maybe_error(n: u64) u64 | Error {

    if n > 10u64 {
        return n;
    }

    return new Error {
        message: "Oh no an error occured"
    };
}

fn give_me_union() u64 | bool | str {
    return false;    
}
