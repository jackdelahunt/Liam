#0

import "stdlib/basic.liam";

alias Foo as u64 | bool;

struct Person {
    foo: Foo
}

fn main(): void {
    let p := new Person{foo: false};

    if p.foo is bool as b {
        println[bool](*b);
    }
}