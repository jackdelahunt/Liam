import "stdlib/basic.liam";
import "lexer.liam";
import "parser.liam";

fn main(): void {
    set_allocator("malloc", 0u64);
    
    let lexer := make_lexer("/Users/jackdelahunt/Projects/Liam/liamc/selfhost/test.liam");
    lex(@lexer);

    for let i := 0u64; i < lexer.tokens.length; i = i + 1u64; {
        let t := array_index[Token](@lexer.tokens, i); 
        println[str](t.string);        
    }
}
