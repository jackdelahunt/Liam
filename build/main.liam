import "/home/jackdelahunt/Projects/Liam/build/std.liam";

fn at(arr: ^[string], index: u64): string {
    insert "return (*arr)[index as usize].clone();";
    return "";
}

fn main(): void {
    let arr := ["1", "2", "3"];
    let x := at(@arr, 2);
    print(@x);
}
