struct Animal {
    name: string
}

struct Person {
    pet: Animal
}

fn print(s: string^): void {
    insert "println!(\"{}\", *s);";
}

fn main(): void {
    let s: string = "this is a string";
    let p: Person = new Person{new Animal{"doggo"}};
    let p2: Person = new Person{new Animal{"cat"}};

    print(@p.pet.name);
}